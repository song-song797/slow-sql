# Slow SQL Web

前端本地运行方式和联调步骤已统一收敛到仓库根目录说明：

- `../../README.md`

当前页面：

- 记录检索页
- 分析结果页

当前前端约定：

- 默认 API 地址：`http://127.0.0.1:10800`
- 默认 API Key：`dev-api-key`
- 不再保留未实现的记录详情页和导出占位功能

默认本地运行命令：

```bash
npm run dev
```
