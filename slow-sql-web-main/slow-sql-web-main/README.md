# Slow SQL Web

前端启动方式和联调步骤已统一收敛到仓库根目录说明：

- `../../README.md`

当前前端默认运行在容器中，由根目录 `docker-compose.local.yml` 统一编排。

当前页面：

- 记录检索页
- 分析结果页

当前前端约定：

- 默认访问地址：`http://127.0.0.1:3000`
- `/api/*` 请求由容器内 Nginx 反向代理到后端容器
- 默认 API Key：`dev-api-key`
- 不再保留未实现的记录详情页和导出占位功能
